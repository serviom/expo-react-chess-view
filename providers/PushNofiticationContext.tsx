import { createContext } from 'react';

export const PushNotificationContext = createContext('');
export const PushNotificationPressContext = createContext({});
export const PushNotificationReceivedContext = createContext({});
